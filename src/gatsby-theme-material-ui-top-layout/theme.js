import { createMuiTheme } from "@material-ui/core/"

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#19857b",
    },
    secondary: {
      main: "#556cd6",
    },
    text: {
      primary: "#000",
      secondary: "#fff",
    },
    background: {
      default: "#DFF9F6",
    },
  },
})

export default theme
